# Google OAuth Configuration
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Customize redirect URI (defaults to /api/auth/callback)
GOOGLE_REDIRECT_URI=/api/auth/callback

# Optional: Base URL for OAuth redirects (auto-detected in production)
BASE_URL=http://localhost:5173

# Session Configuration
# Generate a random secret for signing session tokens
SESSION_SECRET=your-random-session-secret-here

# Database Connection
# Format: postgres://user:password@host:port/database
# For local Docker setup: postgres://colddrive:colddrive@localhost:5432/colddrive?sslmode=disable
POSTGRES_URL=postgres://colddrive:colddrive@localhost:5432/colddrive?sslmode=disable

# Legacy: DATABASE_URL (some tools may use this instead of POSTGRES_URL)
DATABASE_URL=postgres://colddrive:colddrive@localhost:5432/colddrive?sslmode=disable
